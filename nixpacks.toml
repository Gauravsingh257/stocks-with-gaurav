# Nixpacks config for Railway (fallback if Dockerfile is not used).
# Matches .python-version for local parity.

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements-railway.txt"]

[start]
cmd = "python scripts/start_web.py"
