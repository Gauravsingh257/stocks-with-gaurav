# =============================================================================
# Trading Algo — Core Dependencies (Railway-safe, no ta-lib)
# For full local dev (backtest, ta-lib, etc.): pip install -r requirements-full.txt
# =============================================================================

# --- API Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.5.0

# --- Environment & Config ---
python-dotenv>=1.0.0
pydantic-settings>=2.1.0

# --- Scheduling ---
apscheduler>=3.10.0

# --- Trading & Market Data ---
kiteconnect>=4.2.0

# --- HTTP & Networking ---
requests>=2.31.0
httpx>=0.27.0

# --- AI / LLM (content_engine) ---
openai>=1.30.0

# --- Chart generation (content_engine) ---
yfinance>=0.2.40
mplfinance>=0.12.10b0
matplotlib>=3.8.0
playwright>=1.40.0

# --- Utilities ---
orjson>=3.9.0
pandas>=2.1.0
numpy>=1.24.0
