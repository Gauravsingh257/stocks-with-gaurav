FROM python:3.11-slim

WORKDIR /app

COPY requirements-engine.txt .
RUN pip install --no-cache-dir -r requirements-engine.txt

COPY . .

# Use bootstrap: starts /health server FIRST (Railway healthcheck passes),
# then loads and runs the heavy engine in the same process.
CMD ["python", "run_engine_railway.py"]
